pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build_start'
      }
    }

    stage('upload') {
      steps {
        echo 'upload_start'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy_start'
      }
    }

  }
}